<nav>
  <div class="container d-flex flex-row justify-content-between py-3 w-100">
    <ul>
      <li><%= link_to 'Home', root_path %></li>
    </ul>

    <div>
      <h3><%= "Hello, #{ current_user.name || current_user.username }" if current_user %></h3>
    </div>

    <ul class="d-flex flex-row">
      <li class="mx-1"><%= link_to 'Sign Up', new_user_registration_path unless current_user %></li>
      <li class="mx-1"><%= link_to 'Sign In', new_user_session_path unless current_user %></li>
      <li class="mx-1"><%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: "link" if current_user %></li>
    </ul>

  </div>
</nav>

<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
